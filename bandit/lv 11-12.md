# TASK
find the passwd in the ***data.txt*** where all Up and Low letters been rotated 13 positions
# PROCESS
through the hints we can simply think of rot13-encoded data -> use the `tr` cmd
```bash
cat data.txt | tr "a-zA-Z" "z-na-mZ-NA-M"
```
(or simplier way is using decoder online --> ***NOT RECOMMENDED***)
# RESULT
```bash
GROozWPO8QyN0mGrjUkID0WCYkZiQxrN
```
