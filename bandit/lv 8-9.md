# TASK
find the password in the file ***data.txt*** which appears ***ONLY ONCE*** 
# PROCESS
just `cat` it to check the outputs --> `cat data.txt` <br>
--> we will right away see a sequence of characters chaotically organized <br>
so we should remove all the repeated stuffs to filter out the ***ONLY ONCE APPEARED*** passwd
```bash
sort data.txt | uniq -u
```
--> `sort` it by the alphabet so repeated stuffs may sit adjacently in order to be removed by `uniq -u`
# RESULT 
```bash
EjmOSvuAu7sGAHqHVcBDPirRe9T03kxl
```
