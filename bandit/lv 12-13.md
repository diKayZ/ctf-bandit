# TASK 
find the passwd in a ***HEXDUMP-compresesed*** file
# PROCESS
firstly create a temporary dir to contain the contemp file
```bash
mktemp -d
```
then we got the path to that temp dir `/tmp/tmp.hu6AbSVr6o` <br>
nextup i will make a copy from the original file `data.txt` in the temp dir
```bash
cp data.txt /tmp/tmp.hu6AbSVr6o
```
