# TASK
find the passwd that is base64-encoded in the ***data.txt***
# PROCESS
we can use python on Bash to decode the data -> too much effort <br>
instead we have base64 cmd
```bash
base64 -d data.txt
```
(this time we already have the file storing the data to decode)
# RESULT
```bash
pYfOY6HwUsDj5rL9UvyhU7MCmv8vN5Ro
```
