# TASK
find the pass stored in ***data.txt*** which is in ***human-readable*** and ***preceeded by "="***
# PROCESS
check the data type in the file --> `cat data.txt` <br>
we can see many unreadable stuffs, but we have the hint that the pass ***preceeded by "="***
```bash
strings data.txt | grep "="
```
(`strings` is used to operate non-text files instead of `cat` for normal files)
# RESULT
```bash
B0s2khmbT9u0geKuOoVGW3JZKhndE3BG
```
