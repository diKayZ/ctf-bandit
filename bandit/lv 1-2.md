# TASK
find the password in the file named "-" in the home directory
# PROCESS
also get access to the lv2 through 
```bash
ssh bandit1@bandit.labs.overthewire.org -p 2220
```
then type the passwd you got from the previous chal to start this chal <br> then because the name of the file is "-", so it's easy to be mistaken with several options of commands  --> use 
```bash
find . -name "-"
```
it will show the path (link) to the file we need
```bash
./-
```
finally just cat to see the pass in it
```bash
cat ./-
```
# RESULT
```bash
PK8fYLZg2hnHSz83plBL1iEPKdD3QToB
```
