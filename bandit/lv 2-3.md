# TASK
find the password in the file named "--spaces in this filename--" in the home dir
# PROCESS
approach to the host sv like those previous challs 
```bash
ssh bandit2@bandit.labs.overthewire.org -p 2220
```
(from this chall till the end, ill skip this step because it repeats) <br>

use this cmd to find the path to that file
```bash
find . -name "--spaces in this filename--"
```

then we got the path, but because there are spaces in the filename -> should put it in "..." when we cat it
```bash
cat ./"--spaces in this filename--"
```

we got the passwd
```bash
7ZZ2LFrykP2zEyvBl4m3clcL7tGYJPME
```

