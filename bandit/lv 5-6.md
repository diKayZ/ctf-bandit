# TASK
use those cmds to find the password which matches the following requirements 

***human-readable <br>
1033 bytes in size <br>
not executable***

# PROCESS
check the dir `ls -al` then move to ***inhere*** dir `cd inhere` <br>
<img width="400" height="300" alt="image" src="https://github.com/user-attachments/assets/d65fbd64-5123-4637-99c6-d038b05c3764" />

with all these dirs we cannot check one by one to find which matches the req like the pre chall --> takes too much time <br>
we can use cmd find
```bash
find . -type f -size 1033c ! -executable
```
(remember the requirements) <br>
then it shows us the path to the file needed
```bash
./maybehere07/.file2
```
finally cat it then we got the passwd --> `cat ./maybehere07/.file2`
# RESULT
```bash
pXa26xhMWaC2SvDotA4r9EgZkulOeSBW
```
  
